version: "3.4"

services:
  pessoapi:
    container_name: fast_delivery_api
    image: fast_delivery_api
    environment:
      - PORT=5000
    ports:
      - 5000:5000
    build: 
      context: .
      dockerfile: Dockerfile